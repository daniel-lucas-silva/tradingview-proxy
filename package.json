{
  "name": "tradingview-proxy",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "security-check": "npm audit && npm outdated",
    "clean-install": "rmdir /s /q node_modules && del package-lock.json && npm install",
    "reset": "npm run clean-install && npm audit fix --force"
  },
  "dependencies": {
    "express": "4.21.2",
    "axios": "1.10.0",
    "follow-redirects": "1.15.9"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
